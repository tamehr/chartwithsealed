apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: main

resources:
  - ../../secrets/nginx-secret.yaml

helmGlobals:
  chartHome: ../../charts

helmCharts:
  - name: nginx
    releaseName: nginx
    valuesFile: values.yaml
