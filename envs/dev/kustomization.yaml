apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

resources:
  - ../../secrets

helmGlobals:
  chartHome: ../../charts

helmCharts:
  - name: nginx
    releaseName: nginx
    valuesFile: values.yaml
